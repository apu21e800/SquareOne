/* Retired 4 Sept 2026 — carried unverifiable claims ("20+ years", "single
   day"). The residential line lives in DrivewaysBand (home) and /driveways.
   On the delete list; kept as an empty module until Vern removes it locally. */
export default function DrivewayCTA() {
  return null
}
