/**
 * Retired. Superseded by ServicesGrid and the /products index; the old copy
 * carried relationship wording that is not approved for the website. Kept as
 * an empty export until the file is removed in a local commit.
 */
export default function ProductsGrid() {
  return null
}
