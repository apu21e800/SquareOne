/**
 * Retired. This band carried claims Square One has not published (standards,
 * warranties, a client list) and is not rendered anywhere. Kept as an empty
 * export until the file is removed in a local commit; the honesty lint keeps
 * it from ever coming back with the old copy.
 */
export default function WhySquareOne() {
  return null
}
